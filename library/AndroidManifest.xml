<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.michelin.droid"
    android:versionCode="1"
    android:versionName="1.0.00" >

    <!-- library project permission 需要拷贝到主项目AndroidManifest.xml -->
    <!--
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    -->
    
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="10" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/lib_ic_launcher"
        android:label="@string/lib_app_name" >
        
		<!-- library project 应用程序配置 需要拷贝到主项目AndroidManifest.xml -->
		<!--         
		<meta-data
            android:name="error_log_enable"
            android:value="true" />
        <meta-data
            android:name="debug_log_enable"
            android:value="true" />
        <meta-data
            android:name="is_developing"
            android:value="true" /> 
		-->
        
    </application>

</manifest>